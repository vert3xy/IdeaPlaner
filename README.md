# 💡 IdeaPlaner

**IdeaPlaner** — это приватное веб-приложение для пар и небольших групп, предназначенное для структурирования хаоса совместных идей, планов и досуга.

---

## 🚀 Ключевые особенности (MVP)
- **Metadata-Driven Architecture:** Структура категорий и атрибутов управляется через БД, а не захардкожена в коде.
- **Гибкий JSON-хранилище:** Каждая категория имеет свои уникальные поля (жанры для кино, средний чек для ресторанов и т.д.).
- **Безопасность:** JWT-авторизация и хеширование паролей (bcrypt).
- **Динамические фильтры:** Интерфейс подстраивается под типы данных из базы.
- **Mobile-first дизайн:** Адаптивная верстка на Tailwind CSS.

## 🛠 Технологический стек
- **Backend:** Python 3.12, FastAPI, SQLAlchemy
- **Database:** SQLite (с поддержкой JSON1)
- **Frontend:** JavaScript (ES6+), Jinja2, Tailwind CSS
- **Infrastructure:** Linux (Ubuntu 24.04), Systemd, Git
- **Security:** JWT, Bcrypt, Dotenv

## 📦 Архитектура проекта
Проект построен по модульному принципу:
- `routers/` — логика API, разделенная по сущностям.
- `models/` — нормализованная схема БД (Many-to-Many для категорий и атрибутов).
- `static/js/` — реактивная логика фронтенда.
- `templates/` — компонентная структура HTML-шаблонов.